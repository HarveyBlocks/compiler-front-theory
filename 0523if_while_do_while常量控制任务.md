# 0523 if/while/do-while 常量控制任务

## 目标

在不改文法的前提下，利用已有常量计算结果，对 `if / while / do-while` 的条件层做控制流优化。

## 原则

- 不引入 `const_expr`
- 不改成 C/C++ 式复杂左值体系
- 不改现有表达式文法，只用语义层结果做判断
- 优先新增实现，避免破坏既有 translator

## 做法

1. 先复用现有 `ConstantValue` 结果。
2. 在条件类 translator 里判断条件子树是否为编译期常量。
3. 只做保守优化，避免影响 `break / continue / 标签` 绑定。

## 分支策略

### if

- 条件恒真：只保留 then 分支。
- 条件恒假：有 else 就只保留 else，没有 else 就退化为空语句。
- 非常量：保持原逻辑。

### while

- 条件恒假：直接去掉循环体。
- 条件非常量：保持原逻辑。
- 条件恒真：先保守处理，默认保留原结构，不做激进死循环重写。

### do-while

- 条件恒假：保留一次 body，去掉回跳。
- 条件非常量：保持原逻辑。
- 条件恒真：先保守处理，保持原结构。

## 实现顺序

1. 先给 `if` 做恒真/恒假裁剪。
2. 再给 `while` 做恒假裁剪。
3. 最后给 `do-while` 做恒假裁剪。
4. 每一步都补测试，再往下做。

## 测试

- `if(true)` / `if(false)`
- `while(false)`
- `do-while(false)`
- 保留 `break / continue` 的原有行为不变
- 原有非恒定条件 case 不回归

## 暂不做

- 不做函数相关扩展
- 不做路径敏感常量传播
- 不做跨语句全局死代码删除
- 不在文法层新增条件常量语法


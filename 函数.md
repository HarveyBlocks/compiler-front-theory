# 函数

## 目标

在现有显式类型语言的基础上增加函数能力，作为任务2推进。

本阶段目标是：

- 支持函数定义
- 支持参数列表
- 支持 `return`
- 支持函数调用表达式
- 保持现有变量、表达式、控制流能力不被破坏

## 原则

- 不引入 JavaScript 式无类型函数
- 不引入 `const / set`
- 不为了函数去重做整套左值/右值体系
- 可以调整 production，但优先按职责拆分，而不是往旧左部继续堆
- 语法负责“长什么样”，语义负责“是否合法、怎么调用”
- 尽量新增实现，对旧逻辑做适配，不大拆现有功能

## 总体方向

函数应当成为一组新的文法结构，而不是把现有 `stmt / factor / loc` 粗暴混进去。

建议新增以下边界：

- 函数定义
- 参数声明
- 返回语句
- 实参列表
- 调用表达式

## 建议文法结构

下面不是最终代码，只是结构方向。

### 1. 顶层

当前程序是：

- `program -> block`

为了支持函数，建议提升为“顶层项列表”：

- `program -> top_items`
- `top_items -> top_items top_item | epsilon`
- `top_item -> function_decl | block_item`

如果不希望一开始就全面改顶层，也可以先做受限版本：

- `program -> function_list block`

但更推荐直接引入 `top_item`，后续扩展更自然。

### 2. 函数定义

- `function_decl -> type IDENTIFIER OPERATOR_PARENTHESIS_OPEN param_list OPERATOR_PARENTHESIS_CLOSE block`

如果后续要支持无参函数：

- `param_list -> params | epsilon`

### 3. 参数

- `params -> params OPERATOR_COMMA param | param`
- `param -> type IDENTIFIER`

参数本质上是带类型的局部声明，但它属于函数签名的一部分，不建议直接复用普通声明产生式。

### 4. 返回语句

- `return_stmt -> CONTROL_STRUCTURES_RETURN bool OPERATOR_SEMICOLON`

如果后续支持 `void`，再考虑：

- `return_stmt -> CONTROL_STRUCTURES_RETURN OPERATOR_SEMICOLON`

但当前语言没有 `void` 时，不建议提前加。

### 5. 语句接入

函数体中的语句应接入：

- `matched_stmt -> ... | return_stmt`

是否允许 `unmatched_stmt` 直接承接 `return`，取决于现有 dangling-else 结构；通常只要它属于普通已完成语句，挂到 `matched_stmt` 就够了。

### 6. 调用表达式

调用表达式建议作为 `factor` 或与 `factor` 同层的新候选。

例如：

- `factor -> call_expr | ...`
- `call_expr -> IDENTIFIER OPERATOR_PARENTHESIS_OPEN arg_list OPERATOR_PARENTHESIS_CLOSE`
- `arg_list -> args | epsilon`
- `args -> args OPERATOR_COMMA bool | bool`

这里不建议把调用先并入 `loc`。

原因是：

- `loc` 更偏“可定位存储位置”
- 函数调用是取值表达式，不是左值
- 把调用和 `loc` 混在一起会重新把语义边界搞脏

## 为什么不能简单塞进旧结构

原来很多 production 左部职责已经偏重。

如果函数继续直接塞进：

- `stmt`
- `factor`
- `loc`

而不先明确边界，就会出现：

- 声明、调用、返回、控制流继续混挂
- translator 和 semantic callback 越来越依赖字符串分支
- 以后再加数组参数、递归、返回类型检查时更难收束

所以函数阶段允许适度改 production，是合理的。

## 语义阶段建议

函数增加后，语义层至少要新增三类能力。

### 1. 函数符号表

需要独立于变量符号表的函数记录，例如：

- 函数名
- 返回类型
- 参数类型列表
- 是否已定义

不要把函数强行塞进现有变量 `IdentifierRecord`。

变量和函数都叫“标识符”，但它们不是同一种记录。

### 2. 返回检查

需要检查：

- `return` 是否出现在函数体内
- `return` 的表达式类型是否可赋给函数返回类型
- 非 `void` 函数是否满足最基本的返回约束

第一阶段可以先做基础检查，不必一上来做路径完备性分析。

### 3. 调用检查

需要检查：

- 调用目标是否存在
- 实参数量是否匹配
- 实参类型是否可赋给形参类型

## 命令层建议

当前命令系统主要围绕表达式和控制流。

函数加入后，建议新增独立命令语义，不要硬塞进现有赋值/表达式 translator。

第一阶段至少需要考虑：

- 函数入口标签
- 参数装载或形参绑定
- 调用命令
- 返回命令

即使暂时不做完整调用约定，也要先把“函数”和“普通块”区分开。

## 实施顺序

建议按下面顺序推进。

### 阶段1：文法落位

- 引入函数定义产生式
- 引入参数列表
- 引入 `return_stmt`
- 引入 `call_expr`
- 保证旧 case 不回归

### 阶段2：语义落位

- 增加函数记录
- 增加参数作用域注册
- 增加 `return` 类型检查
- 增加函数调用检查

### 阶段3：命令落位

- 增加函数定义和调用的命令表示
- 增加返回命令
- 补基本执行顺序验证

## 测试建议

至少要补这些 case：

- 简单函数定义
- 无参函数
- 单参/多参函数
- 返回值类型正确
- 返回值类型错误
- 调用参数个数错误
- 调用参数类型错误
- 递归调用
- 函数内局部变量与外层同名

## 暂不做

- 不先做闭包
- 不先做匿名函数
- 不先做函数重载
- 不先做默认参数
- 不先做可变参数
- 不先做高阶函数

## 结论

函数任务的正确方向不是“小修小补往旧 expression 里硬塞”，而是：

- 在文法上增加新的职责边界
- 在语义上增加独立函数记录与调用检查
- 在命令层增加独立函数语义

这样既能保住现有功能，又能给后续扩展留出空间。

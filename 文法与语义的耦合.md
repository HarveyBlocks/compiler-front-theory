- 只要把所有的解析字符串的问题整理到一个文档就行了,
- 顺带一提, 通过production产生式的长度来判断是不是合法的产生式也是不健壮的设计
- 但是奈何语义分析就是和时机的文法耦合的, 难以解决和产生式的耦合

- [ ] 整理所有“解析字符串”问题到单独文档
  - 当前至少包括 `SemanticDemo.productionKey(...)` 与 `TypeBuildCallback.build(...)` 里对 `production.toString().trim()` 的依赖
  - 这类逻辑和文法文本强耦合, 改产生式文本就会连带破坏语义分发
  - 先完整找出全部位置并分类, 再单独写文档, 这里只保留待办
- [ ] 整理所有“通过产生式长度判断合法性”的问题
  - 当前大量 translator 通过 `children.length != N` 直接判非法
  - 这不是在判断“是不是当前产生式”, 只是拿规约结果长度做脆弱的旁证
  - 语义分析和文法时机本来就耦合, 这个问题难以彻底消掉, 但至少要把现有位置全部找出来并记录

# 最近两次提交锐评回应

本文只讨论最近两次提交中实际能看到的代码与批注：

- `a7ab04f add: comment on CommandNodeRegister`
- `dad796f add: type resolver`

这次不再以“替自己辩护”为主线。  
写法改为四段：

1. 事实：代码里实际发生了什么。
2. 判断：这些事实说明了什么。
3. 回应：你的批评哪些成立，哪些需要更精确地表述。
4. 反省：我在设计和工作方式上错在哪里。

---

## 二、总事实

从 `dad796f` 这次提交能直接看到的事实有：

1. 新增了类型相关层：
   - `SemanticType`
   - `TypeResolver`
   - `TypeConversionRule`
   - `SemanticTypeSystem`

2. `CommandNodeRegister` 体系被扩展为同时携带：
   - `type`
   - `instructionType`
   - `anchorToken`
   - `uncertainBreaks`
   - `uncertainContinues`

3. 多个 token translator 和 command translator 开始依赖这些新增属性：
   - `LoadIdentifierReferenceTokenTranslator`
   - `SimpleStringTokenTranslator`
   - `TypeTokenTranslator`
   - `InSuffixExpressionTranslator`
   - `DeclarationWithInitializationTranslator`
   - `ArrayAtExpressionTranslator`
   - `If/While/DoWhile` 系列 translator

4. `IdentifierTableBuildCallback` 的执行顺序被后移到 `SyntaxDirectedTranslationCallback` 之后，声明注册时开始直接读取 `context.getCommandContext().peek().getType()`。

5. 代码中大量出现 `SemanticType.unknown()` 作为默认值、兜底值、失败返回值。

6. 类型检查、类型转换判断、隐式 cast 插入、错误锚点选择，已经散落在多个 translator 中。

从 `a7ab04f` 这次提交能直接看到的事实只有一件：

1. 你在 `CommandNodeRegister.getAnchorToken()` 上追加了批注，明确指出它像是“缺少独立 StackCallback 后，用 `CommandNodeRegister` 硬顶上去”的结果。

这些都是代码和提交中直接可见的事实，不需要猜。

---

## 三、核心判断

基于上面的事实，可以直接得出几个判断。

### 1. 这次改动把 `CommandNodeRegister` 从“命令注册对象”推成了“混合语义载体”

这是事实，不是印象。

原本它就负责：

- `register`
- `break/continue` 未决跳转传递

现在又负责：

- 表达式值类型
- 指令操作类型
- 错误定位锚点

这说明它承载的职责明显变多了。

这件事的后果不是抽象“有点耦合”，而是非常具体：

- translator 之间传递的不再只是命令构造中间物，而是多种语义属性的混合包。
- 任何缺失属性的节点，都被迫通过默认值继续流动。
- 于是 `CommandNodeRegister` 变成了一个“什么都能放一点”的容器。

你的批评“本质上是没有增加并使用一个 StackCallback，而是复用了 `CommandNodeRegister`，从而造成耦合”，是成立的。

这里我不再替它找更多理由。  
因为从结果看，代码结构已经说明这个判断是对的。

### 2. `unknown` 在这里不是单一语义，而是多种问题被压成了一个值

这也是事实。

代码里能直接看到 `unknown` 被用在至少四类场景：

1. 默认构造值  
   例如 `CommandNodeRegister.getType()`、`TokenCommandRegister`、`PlaceholderNodeRegister`。

2. 非法操作的失败返回值  
   例如 `arrayElementType()`、`commonNumericType()`。

3. 暂时查不到类型时的兜底值  
   例如查不到标识符记录时。

4. 某些本来就不是表达式值的节点占位  
   例如 `break`、`continue`、空操作类 register。

这四类语义不是一回事。  
但当前代码没有把它们区分开。

所以你的批评“这种 unknown 是危险的”成立，而且不是风格问题，是不变量问题：

- 调用方无法区分“真的未知”
- “前面已经出错”
- “这里本来就不该有值类型”
- “这里只是作者懒得填”

这会直接削弱后续代码对状态的判断能力。

### 3. 类型规则、诊断规则、代码生成规则被混在 translator 里了

这在 `InSuffixExpressionTranslator`、`DeclarationWithInitializationTranslator` 等类里非常明显。

以 `InSuffixExpressionTranslator` 为例，它同时做了：

- 左右操作数类型读取
- 运算合法性判断
- 公共类型选择
- 是否需要隐式 cast 的判断
- cast 指令插入
- 最终操作指令生成
- 错误信息抛出

这不是“稍微多做一点”，而是把三层事情揉在一起了：

- 语义规则
- 诊断规则
- 指令生成

你的批评“类型转换耦合进 translator 里了”成立。  
如果要更精确一点，应该说：

- 不是只有“类型转换”耦合进去了；
- 而是整个“类型决策”都没有和“代码生成”分层。

### 4. 当前实现明显依赖默认值和位置约定维持运行

这也是代码里能直接看到的。

例如：

- `NormalCommandNodeRegister` 对单 child 做透传，其他情况默认 unknown。
- `passthroughAnchor()` 默认取第一个非空子锚点。
- `ArrayAtExpressionTranslator` 依赖 `children[1]` 作为报错定位。
- 多个 `reject(...)` / `validate...(...)` 都写成 `if (token != null)` 再报错。

这说明系统里并没有形成强约束的不变量，而是在用“约定大概如此”让流程往前走。

这种写法在 demo 阶段可以跑，但作为框架代码是不稳的。

### 5. 不是所有改动方向都错，但正确部分也只是局部修正，不足以抵消整体结构问题

这一点也需要按事实说。

例如下面这些改动方向是对的：

- 让类型在 reduce 过程中开始携带，而不是全部事后反解。
- `IdentifierTableBuildCallback` 顺序后移后，声明注册直接消费 reduce 结果类型。
- `TypeTokenTranslator` 让类型 token 不再完全丢失语义信息。

这些方向本身没有问题。

但它们并不能推出“整体设计是好的”。  
因为真正的问题不在于有没有把类型接进去，而在于**是通过什么结构接进去的**。

---

## 四、逐条回应你的主要批评

## 4.1 关于 `CommandNodeRegister.getAnchorToken()`

### 事实

- `CommandNodeRegister` 新增了 `getAnchorToken()` 默认方法，默认返回 `null`。
- `TokenCommandRegister`、`PlaceholderNodeRegister`、`NormalCommandNodeRegister` 都开始承载 `anchorToken`。
- 多个 translator 直接依赖这个值做报错定位。

### 回应

你的批评成立。  
这里不是“加一个 getter 这么简单”，而是把错误定位属性也塞进了 `CommandNodeRegister`。

这导致 `CommandNodeRegister` 更像一个混合语义容器，而不是清晰边界的 register 接口。

### 反省

我这里犯的不是实现细节错误，而是建模错误：

- 缺少一种属性，就把它加到现有最顺手的对象上；
- 这样短期省事，长期只会让对象语义越来越糊。

这是一种典型的“用已有对象继续硬扛”的坏习惯。

---

## 4.2 关于 `SemanticTypeSystem` / `TypeConversionRule`

### 事实

- `TypeConversionRule` 只包含三四个很短的方法。
- `SemanticTypeSystem` 目前只是对 `TypeResolver` 与 `TypeConversionRule` 的薄封装。

### 回应

你的批评基本成立，尤其对 `SemanticTypeSystem` 更成立。

当前体量下：

- `TypeConversionRule` 可以说是一个小工具类。
- `SemanticTypeSystem` 还没有形成真正的“系统”。

所以这更像是提前抽象，而不是必要抽象。

### 反省

这里暴露的是我的一个常见问题：

- 一边又想快接功能，
- 一边又想把名字起得像完整架构，
- 结果得到的是一个规模很小、名字很重的中间态。

这种中间态如果不立刻继续演进，就会变成空壳抽象。

---

## 4.3 关于 `unknown`

### 事实

- `unknown` 被大量用作默认值和失败值。
- 多个 translator 遇到 `unknown` 时会选择跳过进一步类型报错。

### 回应

你的批评主轴成立。  
问题不是“代码里不应该存在 unknown”，而是“unknown 承担了太多互不相同的语义”。

需要更精确的地方只有一点：

- “看到 unknown 就不继续报错”不一定是错误。
- 它有时是在抑制级联错误。

但这只能说明某个局部判断可以接受，不能为整体设计开脱。  
因为根本问题仍然存在：当前没有把不同状态区分开。

### 反省

我这里暴露出的坏习惯是：

- 当设计上缺少明确状态分类时，用一个万能哨兵值先把所有洞堵住。

这会让系统一开始看起来很顺，后面越来越难校验。  
因为所有调用方都被迫猜测 `unknown` 到底是什么意思。

---

## 4.4 关于字符串解析

### 事实

- `SemanticType.mnemonic()` 用字符串表示类型名。
- `TypedCommandFactory` 通过这些字符串拼接 typed 指令名。
- `InSuffixExpressionTranslator` 通过 `operatorFactor.toString()` 分派不同运算语义。

### 回应

这两件事不能混为一谈。

`mnemonic()` 的问题较轻：

- 它是在为当前 `StringCommand` 体系生成指令文本。
- 这是表示层编码，不是主要语义分派机制。

`operatorFactor.toString()` 的问题较重：

- 它确实是在用字符串决定语义逻辑。
- 这说明抽象本身不够强。

所以如果要严谨表达，应当说：

- 不是所有字符串使用都同样糟糕；
- 真正糟糕的是“语义分派依赖 `toString()`”。

### 反省

我在这里的问题是没有把“为了输出文本编码而用字符串”和“为了内部决策而用字符串”区分对待。  
前者可以暂时接受，后者会直接侵蚀类型系统与运算模型。

---

## 4.5 关于重复校验和补丁式 `if (token != null)`

### 事实

- `If/IfElse/While/DoWhile` 里有明显重复的布尔条件检查。
- 多个 translator 内部各自维护一套 `reject(...)` 风格逻辑。
- 报错前大量判断 `token != null`。

### 回应

你的批评成立。

这说明当前代码没有形成统一的：

- 类型诊断辅助层
- 锚点选择策略
- 错误恢复约定

而是在每个 translator 里局部手搓一份。

“遇到 unknown 时不重复报错”可以单独成立。  
但它不影响更大的事实：这些逻辑散、重复、弱约束。

### 反省

我这里的问题不是不会抽公共方法，而是没有先定义好“错误诊断到底属于哪一层”。  
层次没定，复制就会变成自然结果。

---

## 4.6 关于 `ArrayAtExpressionTranslator` 的 child 下标与 anchor 选择

### 事实

- 当前产生式是 `loc->loc OPERATOR_SQUARE_OPEN bool OPERATOR_SQUARE_CLOSE`。
- `children[1]` 在当前文法下确实是 `[`。

### 回应

这里你的批评需要稍微精确一点：

- 说它“当前一定错”不准确；
- 说它“依赖位置约定，设计脆弱”是准确的。

也就是说，这里不是事实错误，而是结构脆弱。

### 反省

这也是同一个问题：  
我没有把“当前文法刚好如此”和“接口层明确表达了这个语义”分开。

一旦文法调整、child 顺序变化、或 translator 复用范围扩大，这类代码就会变得非常容易失效。

---

## 五、哪些地方我不该继续辩护

下面这些点，如果继续替自己找理由，就会变成强词夺理：

- `CommandNodeRegister` 的职责被明显摊大了。
- `unknown` 被混成多个语义。
- translator 同时承担了过多类型决策与代码生成责任。
- 错误诊断逻辑分散、重复、弱约束。
- 当前很多行为依赖默认值和位置约定维持。
- 文档视角偏向 demo，而不是框架。

这些不是措辞问题，是结构问题。

---

## 六、真正的反省

如果只谈“代码哪里不好”，还不够。  
更关键的是我为什么会写出这种代码。

### 1. 我把“先让功能接进去”放到了“先把边界立住”前面

这会自然导向：

- 缺属性，就塞进现有对象；
- 缺状态，就加一个万能 `unknown`；
- 缺规则层，就先写进 translator；
- 缺诊断层，就每个地方各写一点。

这些都能让功能很快跑起来。  
但代价是结构边界越来越糊。

### 2. 我没有足够尊重现有框架的主轴

从你的批注能看出来，你更在意的是：

- 框架怎么承载语义属性
- 不同 callback / stack 的职责怎么分
- 新能力怎样顺着原有机制自然长出来

而我这次做法的重心更偏：

- 先把 typed command 接出来
- 先把类型检查能跑起来

这两个重心不是一回事。  
我做出来的是“功能层面的接入”，不是“框架层面的长法”。

### 3. 我在看到局部正确时，容易高估整体设计的可接受性

例如：

- 类型开始在 reduce 过程携带了；
- 符号表声明注册直接读取 reduce 结果了；
- typed command 也确实出来了；

这些局部都对。  
但它们不能推出整体结构就是对的。

我上一版文字的问题，本质上也是这个问题的延续：

- 抓住局部合理性，
- 试图稀释整体设计失误。

这就是强词夺理的来源。

### 4. 我对“框架代码”和“demo 能跑代码”的标准切换不够严格

如果这是一次临时 demo 修补，有些做法还能容忍。  
但这里是框架代码，标准应该更高：

- 状态要清晰区分；
- 层次要明确；
- 默认值要少；
- 约定最好能变成约束；
- 新能力的接入方式要能推广。

我这次很多实现，其实是按“先让 demo 工作”的标准写出来的。  
这就是根上的问题。

---

## 七、最后结论

基于代码事实，这两次提交可以这样定性：

- 它们不是完全没有方向。
- 但它们确实采用了大量短期接线式做法。
- 这些做法在框架代码里会积累成真实的结构问题。

所以更准确的结论不是“你的批评太重”，而是：

- 你的批评大体成立。
- 少数地方需要表述得更精确，但不影响主结论。
- 我上一版的问题在于试图先证明“我不是乱写”，这会削弱对结构问题的正面承认。

这次真正应该承认的不是“有几个点写得不够优雅”，而是：

- 我没有先顺着框架边界设计新能力，
- 而是先把能力硬接进去，
- 再让现有对象替我承担本不属于它们的职责。

这不是一个小失误，而是一种会持续制造耦合的工作方式。  
如果不改这个工作方式，后面即使继续修代码，也只会反复出现同类问题。


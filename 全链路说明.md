# 全链路说明

这份文档的目标不是罗列源码路径，而是让读者在**不先通读全部源码**的前提下，也能理解这个项目如何工作、如何验证、以及如何扩展。

因此全文尽量按下面的顺序组织：

1. 先说明这一层要解决什么问题
2. 再说明当前实现用了什么设计技巧
3. 再给出关键代码片段
4. 最后用一个小例子说明它在全链路里如何生效

如果只想尽快上手这个项目，建议优先阅读：

- 第 9 到 18 节：文法与分析表是怎么来的
- 第 21 到 40 节：Program 模式下语义分析与测试链路如何工作

如果想扩展这个项目，最常见的入口是：

- 增加 token：看 `ProgramLexicalDemo`
- 增加 grammar：看 `ProgramSyntaxDemo.buildGrammar0()`
- 增加某条规约的命令翻译：看 `SemanticDemo.reduceStrategies0()`
- 增加类型规则：看 `TypeBuildCallback`
- 增加全链路测试：看 `src/main/resources/program-tests` 与 `ProgramSyntaxTestRunner`

## 1. 模块

这一节不是为了背目录，而是先建立“这个项目分几层”的整体视图。

源码目录 `D:\IT_study\source\temp\compiler-front-theory\src\main\java\org\harvey\vie\theory` 当前包含：

- `demo`
- `error`
- `exception`
- `io`
- `lexical`
- `semantic`
- `source`
- `syntax`
- `util`

这些模块大体可以按职责分成四组：

- 输入与错误：`io`、`source`、`error`、`exception`
- 词法系统：`lexical`
- 语法系统：`syntax`
- 语义与演示：`semantic`、`demo`

如果只看 Program 语言这一条主线，可以把它理解成：

```text
source text
  -> io/source
  -> lexical
  -> syntax
  -> semantic
  -> reports / demo output
```

资源目录 `D:\IT_study\source\temp\compiler-front-theory\src\main\resources` 当前包含：

- `demo.txt`
- `grammar.txt`
- `grammar0.txt`
- `grammar0_pool.txt`
- `production_pool.txt`
- `logback.xml`
- `program-tests`
- `serial`

`serial` 目录当前保存：

- `lex_table.data`
- `syntax_table.data`

这两个序列化文件的意义不是“缓存一下而已”，而是：

- 词法状态表和 LR 分析表构造都比较重
- 平时可以复用持久化结果
- 但测试或 grammar 变动时，又可以强制刷新，避免吃旧数据


## 2. 输入资源链路

输入层解决的问题，是把“外部资源”统一抽象成后续前端可消费的字符流，同时保留错误位置。

这里的设计重点有两个：

1. 资源与读取器分层
- `Resource` 代表“某种输入来源”
- `SourceReader` 代表“按字符读取并携带位置信息的读取器”

2. 一开始就把错误上下文带进去
- 这样字符层出错时，不需要临时拼装额外上下文

当前 Program / Demo 常用的是字符串输入，因此落地成：

- `AsciiStringResource`
- `AsciiStringSourceReader`

关键代码片段：

```java
public SourceCharacter read() throws IOException, CompileException {
    offset++;
    int ch = reader.read();
    if (ch == -1) {
        return SourceCharacter.EOF;
    }
    if ((ch & 0xff_ff_ff_00) > 0) {
        errorContext.addError(new IoErrorMessage(offset, "ascii only"));
        throw new CompileException();
    }
    return new AsciiCharacter((byte) ch);
}
```

这个实现说明：

- offset 在最底层就开始累计
- ASCII 限制不是词法阶段才检查，而是输入阶段就直接保证
- 非法字符既写错误，也会中断读取

`peek()` 通过 `mark/read/reset` 实现，这意味着读取器本身支持有限前瞻，而不需要上层每次手工缓存字符。

一个最小例子：

```text
int32 a;
```

输入层不会关心这是关键字还是标识符，它只负责把每个字符按顺序交给词法系统。  
但如果文本里出现非 ASCII 字符，例如中文分号或全角空格，那么错误会先在这里暴露，而不是拖到词法或语法阶段。

如何扩展：

- 如果以后支持文件输入，可以新增 `FileResource`
- 如果以后支持 UTF-8，就应当替换或扩展 `AsciiStringSourceReader`，而不是把字符编码问题留给词法阶段兜底


## 3. 错误对象链路

错误系统的重点不是“有几种异常类”，而是这个项目把**错误消息**和**控制流中断**分开了。

也就是说：

- 有些问题会写入 `ErrorContext`
- 有些问题还会额外抛异常或中止当前流程

这两件事不是一回事。

`ErrorContext` 的作用是：

- 在整个编译链路中统一收集错误
- 让测试、报告、demo 都能在最后看到同一份错误集合

`DefaultErrorContext` 只是最简单的内存实现：

```java
private final List<CompileErrorMessage> errors;

public void addError(CompileErrorMessage message) {
    errors.add(message);
}
```

它刻意不做复杂逻辑，例如：

- 不去重
- 不排序
- 不分类聚合

因为它的职责就是“稳定收集”，而不是“智能处理”。

当前错误消息大致分四层：

- `IoErrorMessage`
- `LexicalErrorMessage`
- `SyntaxErrorMessage`
- `SemanticErrorMessage`

这使得一个全链路报告可以同时看见：

- 输入层失败
- 词法层失败
- 语法层失败
- 语义层失败

一个最小例子：

- 输入里有中文字符，输入层写 `IoErrorMessage`
- 词法无法匹配某个字符序列，写 `LexicalErrorMessage`
- `continue` 出现在 loop 外部，语义层写 `SemanticErrorMessage`

如何扩展：

- 如果以后要做 IDE 集成，可以保留 `ErrorContext` 接口不变，单独增加支持范围、分组、严重级别的实现


## 4. 词法规则构造链路

词法规则入口有两条：

- `D:\IT_study\source\temp\compiler-front-theory\src\main\java\org\harvey\vie\theory\demo\LexicalDemo.java`
- `D:\IT_study\source\temp\compiler-front-theory\src\main\java\org\harvey\vie\theory\demo\program\ProgramLexicalDemo.java`

这一层解决的问题，是把“某种语言有哪些 token”描述成可构造自动机的规则集合。

项目没有把词法规则硬编码进扫描器，而是分成两步：

1. 用 `LexicalPattern` 描述“正则 + token 类型”
2. 再把这一组规则交给 `LexicalDirector` 去导演成状态表

这使得“语言定义”和“自动机构造”分离了。

### 4.1 `LexicalDemo`

`LexicalDemo` 更接近通用演示入口，用来说明：

- 只要给一组 `LexicalPattern`
- 就能生成 DFA 状态表
- 再把状态表喂给 `DefaultLexicalAnalyzer`

### 4.2 `ProgramLexicalDemo`

`ProgramLexicalDemo` 则是 Program 语言的实际落地。

当前正则集合 `REGEX_PATTERNS` 包括：

- `IDENTIFIER`
- `SPACE`
- `COMMENT_BLOCK`
- `COMMENT_LINE`
- `CONSTANT_STRING`
- `CONSTANT_CHARACTER`
- `CONSTANT_INTEGER`
- `CONSTANT_FLOAT`
- `CONSTANT_BOOLEAN_TRUE`
- `CONSTANT_BOOLEAN_FALSE`
- `TYPE_BOOLEAN`
- `TYPE_CHARACTER`
- `TYPE_INT32`
- `TYPE_FLOAT64`
- `TYPE_STRING`
- `OPERATOR_LOGICAL_AND`
- `OPERATOR_LOGICAL_OR`
- `OPERATOR_EQUAL`
- `OPERATOR_NOT_EQUAL`
- `OPERATOR_LESS_EQUAL`
- `OPERATOR_GREATER_EQUAL`
- `OPERATOR_PLUS`
- `OPERATOR_MINUS`
- `OPERATOR_MULTIPLY`
- `OPERATOR_DIVIDE`
- `OPERATOR_LOGICAL_NOT`
- `OPERATOR_LESS`
- `OPERATOR_GREATER`
- `OPERATOR_PARENTHESIS_OPEN`
- `OPERATOR_PARENTHESIS_CLOSE`
- `OPERATOR_ASSIGN`
- `OPERATOR_SEMICOLON`
- `OPERATOR_SQUARE_OPEN`
- `OPERATOR_SQUARE_CLOSE`
- `OPERATOR_BRACE_OPEN`
- `OPERATOR_BRACE_CLOSE`
- `CONTROL_STRUCTURES_IF`
- `CONTROL_STRUCTURES_ELSE`
- `CONTROL_STRUCTURES_WHILE`
- `CONTROL_STRUCTURES_DO`
- `CONTROL_STRUCTURES_BREAK`
- `CONTROL_STRUCTURES_CONTINUE`

`ProgramLexicalDemo.lexicalAnalyzer()`：

1. `new RegexAlphabetCharacterFactory()`
2. `buildTable(alphabetCharacterFactory)`
3. `new SourceAlphabetCharacterAdaptorImpl(alphabetCharacterFactory)`
4. `new DefaultLexicalAnalyzer(table, saca)`

关键代码片段是 `REGEX_PATTERNS`。例如：

```java
new LexicalPattern("int32", ProgramTokenType.TYPE_INT32),
new LexicalPattern("float64", ProgramTokenType.TYPE_FLOAT64),
new LexicalPattern("break", ProgramTokenType.CONTROL_STRUCTURES_BREAK),
new LexicalPattern("continue", ProgramTokenType.CONTROL_STRUCTURES_CONTINUE)
```

这说明新增 token 的第一步，不是去改扫描器分支，而是先把这个 token 的词法规则写进模式集合。

另一个关键点是空白和注释也被当成 token 规则定义出来：

- `SPACE`
- `COMMENT_BLOCK`
- `COMMENT_LINE`

这意味着“忽略它们”并不是词法系统看不见，而是后续阶段按策略过滤。

一个最小例子：

```text
continue;
```

词法层会把它切成：

- `CONTROL_STRUCTURES_CONTINUE`
- `OPERATOR_SEMICOLON`

而不是在语法层再去猜测这个关键字的意义。

如何扩展：

1. 先在 `ProgramTokenType` 增加新 token 类型
2. 再在 `REGEX_PATTERNS` 增加匹配规则
3. 若这个 token 是噪声 token，再决定是否加入后续过滤集合

`buildTable(...)`：

1. 打开 `src/main/resources/serial/lex_table.data`
2. `FLUSH_TABLE` 为真时调用 `buildStatusTable(...)`
3. `new RegexDfaStatusTable.Loader(new ProgramTokenType.Loader(), alphabetCharacterFactory)`
4. `loader.load(is)`

`buildStatusTable(...)`：

1. `new DefaultLexicalDirector(alphabetCharacterFactory)`
2. `director.direct(REGEX_PATTERNS)`
3. `table.store(os)` 写入 `lex_table.data`


## 5. 词法自动机构造链路

这一层的重点不是“会 NFA/DFA 这套教材流程”，而是项目把这套流程做成了可替换的构造管线：

- regex 解析
- regex 树转 NFA
- NFA 转 DFA
- DFA 最小化
- 产出状态表

`DefaultLexicalDirector` 就是在协调这条管线。

关键代码片段：

```java
for (LexicalPattern pattern : patterns) {
    pairs.add(new RegexTypePair(regexParser.parse(pattern.getRegex()), pattern.getType()));
}
NfaStatusGraph<AlphabetCharacter, TokenType> nfaStatusGraph = regexNfaAdaptor.adapt(pairs);
DfaStatusGraph<AlphabetCharacter, TokenType> dfaStatusGraph = nfaDfaAdaptor.adapt(nfaStatusGraph);
return dfaMinimizer.minimize(regexDfaStatusTableFactory, dfaStatusGraph);
```

这段代码清楚地说明了一个设计技巧：  
**词法规则不是直接解释执行，而是先编译成状态机。**

这样做的好处是：

- 扫描阶段更快
- 规则集合可以持久化
- 正则设计与扫描实现分离

一个最小例子：

- `if`
- `identifier`

这两条规则在模式层可能存在前缀关系，但经过统一自动机构造后，最终由状态表来决定最长匹配和类型归属，而不是靠一串手写 `if-else` 判断。

如何扩展：

- 如果以后要支持不同字母表，可以换 `AlphabetCharacterFactory`
- 如果以后要支持不同正则语法，可以换 `RegexParser`
- 不需要碰 `DefaultLexicalAnalyzer` 本体

`direct(List<LexicalPattern> patterns)` 的步骤可以概括为：

1. 对每个 `LexicalPattern`：
   - `regexParser.parse(pattern.getRegex())`
   - 构造 `RegexTypePair`
2. `regexNfaAdaptor.adapt(pairs)`
3. `nfaDfaAdaptor.adapt(nfaStatusGraph)`
4. `dfaMinimizer.minimize(regexDfaStatusTableFactory, dfaStatusGraph)`
5. 返回 `RegexDfaStatusTable`


## 6. 词法扫描链路

扫描阶段的职责，是把输入字符流按 DFA 状态表切成 token 流。

这一层的关键不是“写一个 next()”，而是三件事同时成立：

1. 扫描器不再理解正则，只理解状态表
2. 扫描器要能处理最长匹配结束点
3. 扫描器要把词法错误写进统一 `ErrorContext`

当前入口很简单：

```java
new StatusTableTokenIterator(errorContext, resource.toReader(errorContext), saca, table)
```

真正的核心在 `StatusTableTokenIterator.next0()` 的主循环。可以简化理解成：

```java
read character
  -> adapt to alphabet
  -> move DFA state
  -> if cannot continue, try split current token
  -> reset to start state and continue
```

其中最关键的设计技巧是：**不是每读一个字符就马上产出 token，而是一直推进到“再也走不下去”为止，再回头把前一段切成一个 token。**

这正是自动机扫描的本质。

一个最小例子：

```text
int32 a;
```

扫描过程大致是：

- 读到 `i n t 3 2`，DFA 落在 `TYPE_INT32` 的接受态
- 继续读空格时发现不能再延伸
- 于是把前面的 `int32` 切成一个 token
- 然后从起始态重新开始读 `a`

如果切分点没有接受态，例如：

```text
"unfinished
```

那么 `trySplitToken()` 就会发现当前状态不可接受，于是写入 `LexicalErrorMessage`。

如何扩展：

- 一般不需要碰扫描器本身
- 绝大多数扩展都应该通过“新增词法规则 + 重建状态表”完成
- 只有当字母表模型或错误恢复策略要变时，才需要改这一层


## 7. token 过滤链路

过滤链路存在的原因，是词法层会产出“语法上无意义但诊断上有价值”的 token。

最典型的就是：

- 空白
- 行注释
- 块注释

如果词法层一开始就彻底忽略它们，会有两个问题：

- 不利于调试词法规则是否正确
- 某些诊断或格式化需求以后可能拿不到原始 token

所以当前设计是：

1. 词法层把它们照常产出
2. 语法层之前再按策略过滤

这样“是否保留某类 token”变成一个可配置决策，而不是写死在词法扫描器里。

Program 语言当前过滤掉：

- `SPACE`
- `COMMENT_LINE`
- `COMMENT_BLOCK`

这意味着 Program 语法规则完全不需要为这些噪声 token 留位置。

一个例子：

```text
int32 a;   // comment
```

词法层会先产生注释 token；  
进入语法分析前，过滤层把它丢掉，所以 parser 看到的仍然只是声明语句所需的 token 序列。

如何扩展：

- 如果某种语言保留换行有语义，就不要在这里过滤换行
- 如果要做注释保留或格式化工具，就可以调整过滤策略而不是推翻词法规则


## 8. token 类型链路

`ProgramTokenType` 不只是一个枚举清单，它同时承担了三件事：

1. 给词法规则一个稳定的 token 身份
2. 给错误提示和调试输出提供可读名字
3. 给状态表持久化提供可存储、可加载的编号

因此 token 类型不是“为了写 switch 好看”，而是整个词法/语法系统共享的公共字典。

其中 `priority` 的存在，说明项目允许某些模式发生竞争时按优先级裁决。  
这在关键字与标识符、不同字面量类别之间尤其重要。

一个最小例子：

- `continue` 与普通标识符都能匹配字母串
- 但因为有专门的关键字 token 类型和对应规则，最终它会被识别成 `CONTROL_STRUCTURES_CONTINUE`

如何扩展：

- 新增 token 时，不要只加正则规则
- 必须同时把对应的 `ProgramTokenType` 枚举值加进去，否则持久化、提示和后续语法层都无从对接


## 9. 文法构造链路

文法构造层的目标，是把“人写的 grammar”转成一个统一的内部表示，供 FIRST/FOLLOW、LL、LR、语义注册等多处共用。

这里没有直接手写大文本 parser，而是设计了一套 builder 风格 DSL：

- `define("head")`
- `alternate...`
- `concatenate...`

这套 DSL 的价值在于：

- 写文法时比较接近产生式本身
- 同时又能直接生成结构化对象
- 后续算法不需要重新解析文法字符串

一个典型片段是：

```java
contextBuilder.define("block")
    .alternateTerminal(ProgramTokenType.OPERATOR_BRACE_OPEN)
    .concatenateDefinitionLast("decls")
    .concatenateDefinitionLast("stmts")
    .concatenateTerminalLast(ProgramTokenType.OPERATOR_BRACE_CLOSE);
```

这就等价于：

```text
block -> { decls stmts }
```

设计技巧在于：

- 左部通过 `define(...)` 建立
- 每个 `alternate...` 开启一个新的候选体
- `concatenate...` 则在当前候选体后继续拼接符号

因此文法不是字符串拼接，而是结构化构造。

如何扩展：

- 新增一个非终结符时，从 `define("...")` 开始
- 新增一个分支时，用新的 `alternate...`
- 如果是 Program 语言扩展，优先改 `ProgramSyntaxDemo.buildGrammar0()`


## 10. Demo 文法集合

`ProductionSetContextBuilds` 当前提供多组示例文法：

- `build1`
- `build2`
- `build3`
- `build4`
- `build5`
- `build6`
- `buildSchoolWork1`
- `buildSchoolWork2`
- `buildSchoolWork3`
- `buildSchoolWork4`
- `buildSchoolWork5`
- `buildSchoolWork7`

`build4` / `build5` 使用经典表达式文法：

- `E`
- `T`
- `F`

这个项目保留多套 demo grammar，不是冗余，而是为了分别服务不同阶段：

- 很小的教学文法，便于验证 FIRST/FOLLOW、LL、LR 算法
- Program 文法，便于走完整前端链路

当前真正参与 Program 测试与语义分析的是 `buildGrammar0()`。

它的整体结构可以概括成三块：

1. 块与声明
- `program`
- `block`
- `decls`
- `decl`
- `type`

2. 语句控制流
- `stmts`
- `stmt`
- `matched_stmt`
- `unmatched_stmt`
- `matched_if_stmt`
- `unmatched_if_stmt`
- `matched_while_stmt`
- `unmatched_while_stmt`

3. 表达式层级
- `loc`
- `bool`
- `join`
- `equality`
- `rel`
- `expr`
- `term`
- `unary`
- `factor`

一个关键例子是 `matched_stmt` 中统一容纳：

- 赋值
- while
- do-while
- break
- continue
- block
- matched if

这说明 Program grammar0 不是单纯语法练习，而是已经围绕语义与控制流生成来组织结构了。

如何扩展：

- 如果加一个新语句种类，通常先看它应该挂在 `matched_stmt`、`unmatched_stmt` 还是表达式层
- 然后再决定是否会影响 dangling-else 结构或循环结构


## 11. FIRST 集链路

FIRST 集的作用，不只是给教科书例题服务，而是后面多个算法的公共基础：

- FOLLOW 计算要用它
- LL 表构造要用它
- LR lookahead 传播也要用它

当前项目在实际链路里选用的是迭代不动点实现，而不是朴素递归实现。  
原因很实际：

- 更适合统一处理 epsilon、拼接串、相互递归
- 更容易作为后续算法的稳定基础

它的核心思想就是：

1. 先给所有终结符建立最初 FIRST
2. 再反复扫全部产生式
3. 直到整个 FIRST 映射不再变化

这是一种典型的不动点求解。

如果读者要扩展 grammar，通常不需要改 FIRST 算法本身；  
真正重要的是理解：**只要 grammar 结构合法，这套 FIRST 计算会自动跟上。**


## 12. FOLLOW 集链路

FOLLOW 主要服务两件事：

- LL 表中 epsilon 产生式的落点
- 某些语法分析相关的后继决策

当前实现同样走的是结构化算法，而不是从文法文本上做字符串推导。

可以把它理解成三步：

1. 起始符先拿到输入结束标记
2. 每条产生式内部，把后继串的 FIRST 往前传播
3. 如果后继可空，再把 head 的 FOLLOW 往前传播

这本质上也是一个不动点传播过程。

例子：

```text
A -> B C
```

那么：

- `FIRST(C)` 会影响 `FOLLOW(B)`
- 如果 `C =>* ε`，则 `FOLLOW(A)` 也会继续影响 `FOLLOW(B)`


## 13. LL 预测分析表链路

项目同时保留 LL 预测分析表链路，不是因为 Program 语言靠它运行，而是因为这个仓库本身既是前端工程，也是语法分析算法实验场。

LL 表构造体现的是另一种思想：

- 在分析前把“看到某个输入符号时该选哪条产生式”预先算好

它依赖：

- FIRST
- FOLLOW

填表原则就是经典规则：

- `FIRST(α)` 里的终结符指向 `A -> α`
- 如果 `α` 可空，再用 `FOLLOW(A)` 补表

这一层的价值主要在于：

- 验证 grammar 是否适合预测分析
- 给 TD demo 提供一条独立于 LR 的参考链路


## 14. LR 项集族链路

项集族是 LR 链路的骨架。

它解决的问题是：

- parser 在某个状态下，当前究竟“正在识别哪些产生式的哪一段”

每个 item 可以理解成：

```text
A -> α · β
```

表示：

- `α` 已经看过了
- `β` 还没看

项集族构造的本质，就是不断做：

- `closure`
- `goto`

直到所有状态都被发现。

这里采用 BFS 去扩整个项集族，是因为：

- 状态图天然就是从初始项集不断外扩
- 每个新项集都可能再产生新的 goto 目标

这一层一旦理解，后面的 LR ACTION/GOTO 表就不再神秘，本质上只是“把状态图和规约条件编码成查表数据”。


## 15. Lookahead 链路

lookahead 这层的作用，是把“什么时候可以规约”说得更精确。

如果只有 LR(0) 项，那么：

- 只要看到 `A -> α·`
- 就可能尝试规约

但这通常不够，因为还要知道：

- **在什么输入符号下** 规约才合法

于是项目又显式构造 lookahead 集合，并在项之间传播。

读法可以很朴素：

- 项集族决定“状态长什么样”
- lookahead 决定“这个状态里哪些规约在当前输入下才允许发生”

这层不是 Program 扩展时最常改的地方，但它是 LR 表正确性的关键来源。


## 16. LR 分析表构造链路

LR 分析表是把前面的理论结构最终压成工程上可执行的查表器。

可以把它理解成两张表：

- ACTION：看到某个终结符时做什么
- GOTO：规约出某个非终结符后转到什么状态

其中 ACTION 只有三类核心动作：

- shift
- reduce
- accept

而表项来源很清楚：

- `A -> α·aβ` 说明遇到 `a` 可以 shift
- `A -> α·` 且 lookahead 包含当前终结符，说明可以 reduce
- 增广起始项完成时说明 accept

这层一旦构好，parser 运行时就不需要再理解文法推导细节了，只要查表。

所以 LR 表构造本质上是在做一件事：

- 把 grammar 的结构知识离线计算掉
- 换成运行时可直接查询的状态机表


## 17. LR 分析表持久化链路

分析表持久化的目的很直接：表构造太重，不能每次都从头算。

当前序列化的不是一段“可读文本 grammar”，而是已经编好的表数据，包括：

- 终结符池
- 非终结符池
- ACTION
- GOTO
- 产生式池

因此持久化后的文件更像“编译产物”，而不是源码。

这里的设计取舍是：

- 平时运行可以优先加载已有表，提高速度
- 但在测试或 grammar 改动时，可以强制刷新，保证和当前 grammar 一致

这也是为什么项目里同时保留：

- `buildShiftReduceParsingTable(...)`
- `loadShiftReduceParsingTable(...)`

前者带“必要时重建”的能力，后者则是纯加载。

序列化内容之所以必须包含：

- 起始/接受状态
- 终结符与非终结符池
- 产生式池
- ACTION / GOTO 表

是因为运行期 parser 查表时需要的全部信息都在这里。  
加载之后，`ShiftReducePhaserImpl` 就可以直接调用：

- `activeNext(...)`
- `gotoNext(...)`
- `getProduction(...)`
- `matchTerminal(...)`

而不必重新理解 grammar 的原始构造过程。


## 18. `SyntaxDemo`

`SyntaxDemo` 是前半段算法设施汇流的地方。

它同时扮演三种角色：

1. demo 驱动器
- 给 TD / BU 两条语法分析链路提供可执行入口

2. 表构造器
- 统一封装 FIRST、项集族、lookahead、分析表的构造流程

3. 表缓存门面
- 决定何时直接加载已有表，何时按当前 grammar 强制刷新

关键代码片段：

```java
if (FLUSH_TABLE) {
    table = buildShiftReduceParsingTable0(startHead, context);
    storeTable(table, filename);
}
return loader.load(is);
```

这说明 `SyntaxDemo` 不是单纯的测试壳，而是一个工程入口：  
**既要照顾算法正确性，也要照顾构表成本。**

`buildShiftReduceParsingTable0(...)` 会把前面几节的算法真正串起来：

1. 先算 FIRST
2. 再构造项集族
3. 再传播 lookahead
4. 最后产出 LR 表

而 `buildPredictiveParsingTable(...)` 则走另一条 TD 流程，用来支撑预测分析 demo。

如果读者想验证一套新 grammar 最终能不能跑起来，这一节就是最关键的桥梁。


## 19. 自顶向下语法分析链路

TD 链路在这个项目里的定位，不是 Program 的主执行路径，而是：

- 给 LL 预测分析提供可运行实现
- 作为 LR 链路的对照组
- 帮助读者理解“同一套 grammar 思想换一种分析方式会怎样工作”

它的核心结构比 LR 更直接：

- 一个符号栈
- 一个当前输入 token
- 一张预测分析表

预测分析主循环本质上只做三种事：

1. 栈顶是终结符
- 就和当前输入 token 比较，匹配则消费

2. 栈顶是非终结符
- 就查预测分析表，决定展开哪条产生式

3. 栈顶是结束标记
- 决定是否 accept

这条链路的好处是直观，坏处是对 grammar 约束更强。  
因此它更适合作为教学、对照和局部验证工具，而不是当前 Program 全链路的主承载者。


## 20. 自底向上语法分析链路

BU 链路才是当前 Program 模式真正跑语义分析的主路径。

它的核心分层很清楚：

1. `ShiftReducePhaseContext`
- 保存 LR 状态栈、过滤后的 token 迭代器、错误上下文

2. `ShiftReducePhaserImpl`
- 依据 LR 表驱动 shift / reduce / accept

3. `ShiftReduceSemanticContext`
- 在相同的 shift/reduce 时机上挂接语义系统

关键代码片段：

```java
while (true) {
    SourceToken current = ctx.currentToken();
    int top = ctx.peek();
    ActiveTableElement element = table.activeNext(top, table.matchTerminal(current));
    if (element == null) {
        context.onError(ShiftReduceErrorType.UNDEFINED_ACTION);
    } else if (element.isShift()) {
        onShift(context, element, current);
    } else if (element.isReduce()) {
        onReduce(context, element);
        if (element.isAccept()) {
            break;
        }
    }
}
```

这段循环本质上只做一件事：  
**查表决定动作，然后把动作时机交给语义系统。**

它自己不负责：

- 建语法树
- 做类型
- 生成命令

这些都留给后面的 callback 总线去做。

一个最小例子：

```text
a = 1;
```

在 LR 主循环里会经历：

- 若干次 shift：接收 `a`、`=`、`1`、`;`
- 若干次 reduce：规约出 `loc`、表达式、语句
- 最后 accept

而每次 shift/reduce 又会同步驱动后面的语义系统，所以语法分析和语义分析并不是两个完全断开的阶段，而是**同一条事件流上的两层工作**。


## 21. 语义回调注册链路

这一层真正解决的问题不是“把几个 callback 串起来”，而是把**语法分析的时机**和**语义工作的种类**拆开。

在 LR 里，天然只有三种关键时机：

- shift：一个 token 刚被接受
- reduce：一条产生式刚被规约
- accept：整棵分析完成

如果所有语义逻辑都直接塞进 parser，本质上会变成一个难以维护的大函数：建树、建符号表、做类型、生成命令、报错，全都挤在一起。当前框架故意不这么做，而是把这些工作拆成多条并行“语义通道”：

- 语法树通道：保证后续阶段有稳定的结构可读
- 作用域通道：处理 `{` 进入与 `block` 退出
- 类型通道：给确实有值的节点绑定类型属性
- 命令通道：把语法结构翻译成中间命令树
- 标识符通道：在合适的规约点做使用检查与声明登记
- 结果提取通道：在 accept 前把内部结构拍平成最终结果

这就是 `ShiftReduceCallbackRegister` 的意义：它不是“回调列表”这么简单，而是**把一轮 shift/reduce 的生命周期广播给多条语义通道**。  
测试模式与展示模式只是在这条总线上挂不同的 callback 组合：

- 简化模式：只建树或只做最少语义
- 测试模式：返回结构化结果而不是打印
- 演示模式：更强调可观察输出

关键代码片段：

```java
public static ShiftReduceCallbackRegister buildShiftReduceTestRegister() {
    ShiftReduceCallbackRegister register = new ShiftReduceCallbackRegisterImpl();
    register.add(new TreeBuildCallback());
    register.add(instanceIdentifierScopeCallback());
    register.add(new TypeBuildCallback());
    register.add(new SemanticResultCallback());
    register.add(instanceSyntaxDirectedTranslationCallback());
    register.add(instanceIdentifierTableBuildCallback());
    register.add(new PassiveErrorCallback());
    return register;
}
```

这个顺序本身就体现了设计：

- 先建树，否则后面没有稳定结构可读
- 再处理作用域与类型，因为后面命令和声明登记都依赖它们
- 再做命令翻译与结果提取

一个最小例子是：

```text
{
  int32 a;
  a = 1;
}
```

在这段输入上，同一次 reduce 会同时驱动多条通道：

- 语法树通道得到 `decl`、`stmt`、`block`
- 作用域通道在 `{` 进入，在 `block` 退出
- 类型通道给 `type`、`loc`、`bool` 等节点绑定类型
- 命令通道生成赋值相关命令
- 标识符通道登记 `a`


## 22. `ShiftReduceSemanticContext`

`ShiftReduceSemanticContext` 的角色不是“什么都往里塞的大对象”，而是**这几条语义通道共享的会合点**。

它同时保存三类东西：

1. 语法推进状态
- 当前 LR 状态栈
- 当前输入位置
- 当前 callback 迭代进度

2. 被多条通道共享的语义中间态
- `TreeContext`
- `CommandContext`
- `TypeContext`
- 作用域/标识符表

3. 可复用的类型判断能力
- `TypeResolver`
- `TypeConversionRule`

设计重点不在“字段很多”，而在**谁应该是上下文里的公共能力，谁不应该**：

- 语法推进必须在这里，因为所有 callback 都依赖同一套 parser 状态
- 语义栈必须在这里，因为建树、建命令、读类型都要对准同一次规约
- 类型规则对象可以在这里，因为它们是纯服务，不携带一次规约的局部状态
- `break/continue` 这种局部控制流信息则没有必要放在 context 单独维护，它跟某棵子树绑定更自然

它还承担一个很重要但容易被忽视的职责：**把“缺值”和“错误”分开**。

- 某些节点本来就没有值，例如 `block`、`break;`、`continue;`
- 这些节点在 `TypeContext` 里没有类型绑定，是正常情况
- 但当某个位置“必须有类型”却取不到类型时，调用方要立刻报错，而不是默认兜底

所以 `TypeContext` 的“无绑定”是语义事实，不是错误恢复手段。

关键代码片段可以压缩成下面这个样子来理解：

```java
private final TreeContext treeContext = new TreeContext();
private final CommandContext commandContext = new CommandContext();
private final TypeContext typeContext = new TypeContext();
private final TypeResolver typeResolver = new TypeResolver();
private final TypeConversionRule typeConversionRule = new TypeConversionRule();
private final IdentifierTableBuilder identifierTableBuilder = new IdentifierTableBuilder();
```

这里能看出一个重要技巧：  
**把“数据存储”与“规则服务”都挂到同一个语义上下文上，但局部控制流问题不放这里。**

例子：

- `TypeResolver` 这种对象是纯规则服务，谁都可以来问
- `TypeContext` 是一次分析过程里的节点属性仓库
- `break/continue` 则属于具体子树的未决信息，所以不该提升到上下文全局


## 23. `BuildStackContextCallback<T>`

这个抽象的价值，在于把“凡是基于规约建立某种语义栈”的共性都提出来。

对语法树、命令树这类通道来说，模式完全一样：

1. shift 时，根据 token 造一个叶子产物
2. reduce 时，弹出若干孩子
3. 用 `production + children` 造一个新节点
4. 再把这个结果压回对应的语义栈

如果没有这层抽象，那么：

- `TreeBuildCallback` 要自己写一遍
- `CommandBuildCallback` 也要自己再写一遍
- accept 前最后一次 reduce 还要再抄一遍

这会把大量精力浪费在“搬运栈元素”而不是“定义语义规则”上。

所以它真正抽掉的是**规约驱动构造器的共性骨架**，让具体子类只关心：

- shift 时怎么造叶子
- reduce 时怎么造父节点
- 用哪一个语义栈承载结果

这是一种很典型的模板方法思路。

关键代码片段：

```java
public void onShift(ShiftReduceSemanticContext context, int nextStatus, SourceToken token) {
    Stack<T> stackContext = supplier.getStackContext(context);
    T item = supplier.instanceNodeOnShift(context, token);
    stackContext.push(item);
    ShiftReduceCallback.super.onShift(context, nextStatus, token);
}

public void onReduce(ShiftReduceSemanticContext context, SimpleGrammarProduction production) {
    T item = reduceProduction(context, production);
    Stack<T> stackContext = supplier.getStackContext(context);
    stackContext.push(item);
    ShiftReduceCallback.super.onReduce(context, production);
}
```

它不是在定义“语义规则”，而是在定义“基于 shift/reduce 建栈”的通用骨架。

例子：

- `TreeBuildCallback` 用这套骨架把 token 变成 `TokenNode`，把规约结果变成 `HeadNode`
- `CommandBuildCallback` 用同一套骨架把 token/production 翻译结果压成 `CommandNodeRegister`


## 24. 语法树链路

语法树通道的核心目的不是“为了好看把语法树建出来”，而是给后续所有语义阶段一个**稳定、可回看的结构载体**。

为什么需要它：

- 类型绑定要挂在具体节点上
- 标识符声明要回看规约出来的 `type` 子树和 `IDENTIFIER`
- 命令翻译也经常要知道当前 reduce 的结构长什么样
- 报错定位需要从某棵子树中找合适的 token 作为锚点

因此这里不是做抽象 AST，而是保留贴近规约现场的 shift-reduce 语法树：

- token 变成叶子
- reduce 变成 `HeadNode`
- 某些带额外语义的结构，例如 block，可以在出作用域时替换成更合适的节点

这棵树的定位不是“最终用户语义树”，而是**LR 现场的共享结构层**。  
后面的 `TypeContext` 之所以能做“节点到类型”的绑定，前提就是这棵树存在。

一个最小例子：

```text
a = 1;
```

在 reduce 过程中，语法树不会直接变成“赋值语句对象”，而是先保留出这样的结构关系：

- `loc`
  - `IDENTIFIER`
- `bool`
  - ...
- `matched_stmt`
  - `loc`
  - `=`
  - `bool`
  - `;`

后续类型系统与命令系统都可以沿着这棵树回看“第几个孩子是什么”。


## 25. 作用域链路

作用域处理的设计重点是：**进入作用域和退出作用域发生在不同的 parser 时机**。

进入作用域依赖 token：

- 看到 `{` 就已经进入新的声明区域了

退出作用域依赖 reduce：

- 只有 `block` 规约完成，才能确定这个块里有哪些声明
- 这时再把当前作用域完整弹出，才能把结果附着回这个 block 节点

因此这里故意不是“看到 `}` 就弹作用域”，因为那样太早了。  
正确时机是：

- shift `{`：`scopeInto()`
- reduce `block`：`scopeExist()`

这背后有个设计技巧：**token 决定边界的开始，产生式决定边界的完成**。  
当前 `IdentifierScopeCallback` 正是在利用这两个时机差。

关键代码片段：

```java
public void onShift(ShiftReduceSemanticContext context, int nextStatus, SourceToken token) {
    if (scopeIntoPredicate.test(token)) {
        context.scopeInto();
    }
    ShiftReduceCallback.super.onShift(context, nextStatus, token);
}

private void onReduce0(ShiftReduceSemanticContext context, SimpleGrammarProduction production)
        throws CompileException {
    if (!scopeExistPredicate.test(production)) {
        return;
    }
    IdentifierRecord[] scope = context.scopeExist();
    treeContext.resetTop(top -> {
        ShiftReduceSyntaxTreeNode replaced = top.toHead().instanceBlock(scope);
        context.moveTypeBinding(top, replaced);
        return replaced;
    });
}
```

这里还有一个很好的细节：

- `block` 节点被替换时，类型绑定也跟着迁移
- 说明节点替换不是只改树形结构，还要维护附着在节点上的属性


## 26. 标识符表链路

标识符通道做的是两件事，而且必须区分：

1. 使用点检查
- 一个 `loc -> IDENTIFIER` 规约完成时，就可以检查它在当前可见作用域里是否存在

2. 声明点登记
- 一个 `decl` 规约完成时，才能把“名字 + 类型 + 是否初始化”作为一条完整记录写入符号表

这里的关键设计不是“写个 callback 去查表”，而是**把“声明何时完整”和“使用何时可判定”分开**：

- 使用点不需要等类型系统全部做完，只要名字解析可见就能判
- 声明点则必须等 `type` 子树已经得到类型绑定，否则登记不完整

所以 `IdentifierTableBuildCallback` 会在声明时显式读取 `typeHeadNode` 上的 `TypeRegister`。  
这说明符号表与类型系统不是彼此独立的两条线，而是：

- 名字解析主要依赖作用域
- 声明记录的形成又依赖类型通道先完成


## 27. `IdentifierTableBuilder`

这个类的设计重点不是“有个 list 存作用域”，而是它同时管理两套编号语义：

- `no`：全局唯一记录编号
- `offset`：当前作用域内的相对编号

这样做的好处是：

- `no` 适合做稳定引用
- `offset` 更接近块级变量布局或作用域内顺序

当前结构本质上是一个作用域栈：

- 进入 block 就压入新作用域
- 离开 block 就弹出当前作用域
- 名字查找从栈顶往下找，天然实现“内层遮蔽外层”

这里没有搞复杂索引结构，因为当前语言规模不大，而“从内向外线性查找”的语义非常直接，也便于验证作用域行为是否正确。


## 28. 命令构建链路

命令通道的核心思路不是“每次规约顺便吐几条字符串命令”，而是把命令生成拆成两层：

1. token 级翻译
- 一个 token 自己就能决定的局部行为

2. production 级翻译
- 只有在若干子结构都已经齐备后，才能决定怎么组合

这就是 `TokenTranslatorStrategy` 与 `CommandTranslatorStrategy` 分开的原因。

为什么要分：

- `IDENTIFIER`、字面量、`break`、`continue` 在 shift 时就有局部语义
- `while`、赋值、数组访问、二元表达式只能在 reduce 时看完整孩子结构后再决定

这样做的收益是，命令生成逻辑与 LR 的两个关键时机天然对齐：

- shift 处理叶子语义
- reduce 处理组合语义

而 `CommandBuildCallback` 只负责搭骨架，不负责具体翻译规则。

关键代码片段：

```java
private static ShiftReduceCallback instanceSyntaxDirectedTranslationCallback() {
    TokenTranslatorStrategy shiftStrategies = shiftStrategies();
    CommandTranslatorStrategy reduceStrategies = reduceStrategies0();
    return new CommandBuildCallback(shiftStrategies, reduceStrategies);
}
```

这段代码很短，但已经把设计说透了：

- shift 用 token translator
- reduce 用 production translator
- `CommandBuildCallback` 只做调度，不直接写具体语义

例子：

- `IDENTIFIER` 在 shift 时就能翻译成“装载某个引用”
- `while (bool) stmt` 必须等 reduce 时才能知道条件和循环体都齐了


## 29. token translator 链路

token translator 这一层应该尽量“薄”，但不是没意义。

它处理的是**单个 token 就足以成立的语义单位**：

- 字面量：把值压入命令流
- 标识符：生成对变量引用的装载
- `break` / `continue`：生成未决跳转
- 类型 token：为后续声明或转换保留基础语义

为什么这些逻辑适合放在 shift：

- 它们不需要等待兄弟节点
- 它们一旦错过 shift，再往后也不会获得更多上下文

所以 token translator 的定位不是“把所有 token 都翻译掉”，而是：

- 能在叶子层定下来的，尽早定
- 必须依赖组合关系的，留给 reduce


## 30. production translator 链路

production translator 才是真正承载“语义规则”的地方。

一个 translator 之所以对应某类产生式，不是因为“这个类名好看”，而是因为它在表达一个**结构性的语义动作**，例如：

- 语句列表怎么串接
- 赋值怎么检查类型并插入必要转换
- `while` 怎么产生控制流骨架
- `if-else` 怎么安排条件分支与跳转
- 数组访问怎么先求引用再求索引

这一层本来就不可能完全摆脱文法，因为 reduce 时机本来就是由产生式决定的。  
真正应当避免的不是“与产生式耦合”，而是“用脆弱方式表达这种耦合”，例如靠生产式打印字符串或孩子数瞎猜。

也就是说，这里的正确目标是：

- 接受语义动作依赖产生式结构
- 但尽量把这种依赖写成稳定、集中、可读的形式


## 31. `CommandNodeRegister`

这是当前命令系统里最关键、也最容易被低估的一层。

它解决的不是“把命令节点包一层接口”，而是**把“当前节点怎么向外注册命令”与“当前节点内部还带着哪些未决信息”绑定在一起**。

它至少承载两类责任：

1. 输出责任
- `register(CommandNodeBuilder outer)` 表示“把我自己的命令结构注册到更外层 builder”

2. 悬而未决的信息
- 未绑定目标的 `break`
- 未绑定目标的 `continue`

如果没有这一层，控制流相关的未决信息就只能另找地方存：

- 存在全局 context 里，容易混层
- 或者在 translator 之间手工传列表，容易散

现在的设计是：**一个语义子树对应一个 `CommandNodeRegister`，它既能输出命令，也能携带这棵子树还没处理完的控制流问题**。  
这是一个很强的设计点。

关键代码片段：

```java
public interface CommandNodeRegister {
    void register(CommandNodeBuilder outer);

    default List<UncertainLabelGotoCommand> getUncertainBreaks() {
        return List.of();
    }

    default List<UncertainLabelGotoCommand> getUncertainContinues() {
        return List.of();
    }
}
```

这比“直接返回一组命令字符串”强很多，因为它允许：

- 正常输出命令结构
- 同时携带还未决的控制流信息

例子：

- 一个普通字面量 register 只需要负责输出
- 一个含 `break;` 的语句体 register 除了输出，还会携带未绑定的 break 跳转


## 32. 命令树链路

这里故意先建“命令树”，而不是 reduce 一次就直接往最终 `List<String>` 里 append。

原因很实际：

- label 的目标位置在 flatten 前并不知道
- `if`、`while`、`do-while` 这类结构天然是嵌套的
- `break/continue` 的绑定也依赖后面某个更外层结构来补完

所以当前命令系统分成两步：

1. 先建立结构化命令树
- `HeadNode` 表示一个组合片段
- `TerminalNode` 表示一条具体命令
- `LabelNode` 表示一个位置锚点

2. 最后统一拍平
- 深度遍历树
- 遇到 `LabelNode` 时给 label 记最终索引
- 遇到 `TerminalNode` 时输出实际命令

这和语法树一样，都是“先保留结构，后线性化”的思路。


## 33. 三地址命令链路

最终的三地址命令并不是一开始就存在的，它是命令树拍平后的结果。

当前这层有三类核心对象：

- 普通命令
- label
- 带未决 label 的跳转命令

这里的关键技巧是：**先允许跳转目标未定，再在更外层结构补上**。  
这使得循环和条件分支的翻译可以自然表达，不必为了“目标行号暂时不知道”而提前线性化。

`ThreeAddressCodePrinter` 的角色也因此很明确：它不负责语义，只负责把已经定型的命令对象转成稳定文本输出。


## 34. `break / continue` 链路

这一块的核心思路是：`break` 和 `continue` 在词法上很简单，但在语义上是**延迟定目标的跳转**。

一个 `break;` 在它出现的那一刻，只知道：

- 这是一条跳转
- 但不知道应该跳到哪一个 loop 的结尾

`continue;` 也一样：

- 知道要回某个 loop
- 但不知道是回条件检查点，还是回循环头，具体取决于外层是 `while` 还是 `do-while`

所以当前做法不是立刻求值，而是分三段：

1. 产生未决跳转
- shift 到 `break` 或 `continue` 时先造 `UncertainLabelGotoCommand`

2. 随子树向上传递
- `NormalCommandNodeRegister` 聚合所有孩子还没解决的 `break/continue`

3. 到正确的 loop translator 再统一绑定
- `while` 绑定到循环头 / 循环尾
- `do-while` 绑定到条件检查点 / 循环尾

这比全局列表更局部化，也更贴合“哪棵子树里出现了这些未决跳转”这个事实。

程序最外层的 `ProgramCommandTranslator` 再做最后一道兜底检查：

- 如果到 program 顶层还有未决 `break/continue`
- 说明这些语句没有被任何 loop 吸收
- 于是直接报错

关键代码片段可以看 `WhileStatementTranslator`：

```java
SemanticLabel whileStartLabel = new DefaultSemanticLabel();
SemanticLabel whileEndLabel = new DefaultSemanticLabel();
thisBuilder.add(new LabelNode(whileStartLabel));
children[2].register(thisBuilder);
thisBuilder.add(new TerminalNode(CommandFactory.ifnGoto(whileEndLabel)));
children[4].register(thisBuilder);
thisBuilder.add(new TerminalNode(CommandFactory.gotoCommand(whileStartLabel)));
thisBuilder.add(new LabelNode(whileEndLabel));
bindLoopLabels(children[4], whileStartLabel, whileEndLabel);
```

这个顺序就对应一段很直白的三地址逻辑：

```text
Lstart:
  eval condition
  ifn_goto Lend
  body
  goto Lstart
Lend:
```

而 `bindLoopLabels(...)` 的意义就是：

- body 里所有尚未绑定的 `continue`，现在知道该去 `Lstart`
- body 里所有尚未绑定的 `break`，现在知道该去 `Lend`


## 35. 类型链路

类型系统当前最重要的设计决策是：**类型不是一条平行栈，而是节点属性**。

这背后的原因是：

- 在 LR 里，真正稳定的对象是“某次规约得到的节点”
- 类型的拥有者也应该是这个节点，而不是一个容易错位的镜像栈槽位

因此当前方案是：

1. `TreeBuildCallback` 先保证 reduce 后一定有一个具体语法树节点
2. `TypeBuildCallback` 在同一次 reduce 上决定这个节点是否有类型属性
3. 若有，就把 `TypeRegister` 绑定到该节点
4. 后续任何阶段再通过节点回取这个类型

这里最关键的区分是：

- **无值**：这个节点本来就不应有类型，例如 `block`、`break;`
- **缺类型**：这个位置按语义必须有类型，但实际没取到

前者合法，后者必须报错。

所以 `TypeContext` 里的“没有绑定”只表示“这个节点没有值”，并不自动表示错误。  
真正需要类型的地方，会显式调用：

- `requireType(...)`
- `TypeAttributes.childType(...)`

一旦取不到，就立刻失败。

`TypeRegister` 之所以同时存 `type`、`instructionType`、`anchor`，是因为这三件事语义上不同：

- `type`：表达式最终是什么类型
- `instructionType`：参与某条运算指令时应以什么类型执行
- `anchor`：如果出错，应当报在哪个 token 上

例如比较表达式的最终结果是 `boolean`，但它的运算过程可能要先把左右值提升到共同的数值类型。  
这时 `type` 和 `instructionType` 就不能混为一谈。

当前 `TypeBuildCallback` 的实现方式仍然依赖具体产生式分发，这属于落地层面的现状；  
但核心设计思路其实是：

- 类型属性只绑定到真正有值的节点
- 缺值不是错误
- 需要类型的地方必须显式要求它存在
- 隐式转换是否允许、是否需要插入 cast，统一由 `TypeConversionRule` 判

当前赋值和初始化语句的处理正是这个思路：

1. 先判断源类型能否赋给目标类型
2. 若不能，直接报错
3. 若能但编码表示不同，再显式插入 `st_top_cast`
4. 最后生成赋值命令

关键代码片段：

```java
public void onReduce(ShiftReduceSemanticContext context, SimpleGrammarProduction production) {
    HeadNode head = currentReducedHead(context);
    TypeRegister result = build(context, production, head);
    if (result != null) {
        context.bindType(head, result);
    }
    ShiftReduceCallback.super.onReduce(context, production);
}
```

它说明两件事：

- reduce 完成后，类型是绑在“当前语法树节点”上的
- `result == null` 不是异常，而是“这个节点没有值”

再看一个具体例子：

```text
int32 a;
a = 1.5;
```

这里赋值阶段会问两个问题：

1. `1.5` 的类型能否赋给 `a` 的类型
2. 如果能，是否需要插入显式 cast

当前规则下，这种从 `float64` 到 `int32` 的赋值不会被接受，因此不会进入“插入 cast 后继续”的分支。

再看另一个例子：

```text
float64 x;
x = 1;
```

这里是允许的，但不会假装“天然没事”，而是会显式插入 `st_top_cast(int32, float64)`，因为编码表示确实发生了变化。


## 36. 结果提取链路

`SemanticResultCallback` 的职责不是“最后 print 一下”，而是把前面所有内部结构收束成对外结果。

为什么一定要等到 `beforeAccept`：

- 在 accept 前，顶层命令树才完整
- 这时所有 label 才能被拍平成稳定索引
- 标识符表也已经走完所有 block 的进出

因此它做的事是：

1. 取出唯一顶层 `CommandNodeRegister`
2. 向一个新的外层 builder 注册整棵命令树
3. 拍平为线性命令
4. 打印成文本
5. 连同标识符记录一起封装成结果

这里的设计技巧是：**对外暴露的结果尽量简单，对内可以保留复杂结构到最后一刻**。


## 37. `SemanticAnalysisResult`

这个结果对象有意保持很薄，只暴露两样最终产物：

- 命令文本
- 标识符记录

它不把内部的语法树、命令树、类型属性、未决跳转等中间状态继续暴露出去。  
原因很直接：

- 那些中间结构主要服务于分析过程
- 对测试和报告而言，真正需要的是最终行为结果

所以它承担的是“分析结果的稳定出口”，而不是“内部全过程快照”。


## 38. 测试链路

测试链路的设计目标不是“跑通几条样例”，而是**把整个前端从词法到语义当成一个整体来验**。

因此 `ProgramSyntaxTestRunner` 做的不是只测某个函数，而是每次完整执行：

- 词法分析
- 语法分析
- 语义分析
- 命令生成
- 结果汇总

这里有两个关键点：

1. 每次按当前 grammar 重建或刷新分析表
- 避免测试偷偷吃旧的序列化表
- 保证 grammar 改了，测试一定跟着改后的 grammar 跑

2. 报告区分三件事
- `Expected`
- `Observed`
- `Matched`

这比简单的 PASS/FAIL 更诚实，因为有些错误并不是抛异常，而是写进 `errorContext`；  
测试必须把“观察到什么”与“期望是什么”分开说清楚。

关键代码片段：

```java
boolean expectedFailure = caseName.contains("invalid");
boolean hasErrors = !errorContext.isEmpty();
boolean observedRejected = failure != null || hasErrors;
boolean observedAccepted = executedSuccessfully && !hasErrors;
boolean matchedExpectation = expectedFailure ? observedRejected : observedAccepted;
```

这段逻辑的价值在于，它不把“抛异常”误当成唯一失败信号。  
对前端项目来说，失败可能有两种：

- 抛异常
- 语义检查把错误写进 `errorContext`

例子：

- `continue` 出现在 loop 外部，可能直接拒绝
- 数组下标类型不对，也可能是通过 `errorContext` 体现

测试框架需要把这两种都算进 `Observed`。


## 39. 报告输出链路

报告系统的意义不是“留个日志”，而是让语义错误、控制流结果、符号表状态都能被**事后审计**。

单用例报告之所以同时输出：

- 源码
- 生成命令
- 符号表
- 错误列表
- 异常

是因为很多前端错误并不只会体现在断言失败上。  
例如：

- 跳转目标行号是否正确
- 命令顺序是否合理
- 某个变量是否落在预期作用域

这些都必须靠完整报告才能复盘。

汇总报告则提供另一层视角：

- 一次批量运行里哪些 case 匹配预期
- 每个 case 产出了多少命令、多少错误、多少符号表项

也就是：单报告看局部逻辑，汇总报告看整体健康度。


## 40. Program 主链路

如果把 Program 模式从头到尾压成一句话，它的真实工作流是：

1. 读取源文本，确保输入字符合法
2. 词法分析把字符流切成 token 流
3. 用当前 grammar 构造或刷新 LR 分析表
4. LR parser 执行 shift / reduce
5. 每次 shift / reduce 时，同步驱动多条语义通道
6. 这些通道分别累积出：
   - 语法树
   - 作用域与符号表
   - 类型属性
   - 命令树
7. accept 前把命令树拍平成三地址命令
8. 把符号表与命令文本打包成最终结果
9. 测试层再把结果与错误上下文一起写入报告

如果再压缩成设计层面的三句话，就是：

- parser 决定时机
- callback 框架把不同语义工作并行挂到这些时机上
- 真正对外可见的结果，只在最末端统一收束

这才是当前“全链路”的核心思路。

如果要扩展 Program 语言，一个实用顺序是：

1. 先在 `ProgramLexicalDemo` 增加 token
2. 再在 `ProgramSyntaxDemo.buildGrammar0()` 增加 grammar
3. 若这个结构需要命令语义，在 `SemanticDemo.reduceStrategies0()` 挂上 translator
4. 若这个结构会产生值或参与类型检查，在 `TypeBuildCallback` 增加对应规则
5. 最后补一条 `program-tests/*.txt` 全链路用例，并看生成报告

例如这次给 `continue` 补链路，就是按这个顺序完成的：

1. 词法已有 `CONTROL_STRUCTURES_CONTINUE`
2. grammar 增加 `matched_stmt -> continue ;`
3. reduce strategy 把它接到 `simpleShrink`
4. token translator 负责生成未决 `continue` 跳转
5. `while` / `do-while` translator 在更外层完成绑定
6. 再用专门的测试文本检查命令顺序与跳转目标是否正确


## 41. 资源文件

当前测试输入目录：

`D:\IT_study\source\temp\compiler-front-theory\src\main\resources\program-tests`

当前用例文件：

- `text1.txt`
- `text2.txt`
- `text3.txt`
- `text4-unary-invalid.txt`
- `text5-sibling-scope.txt`
- `text6-break-before-inner-while.txt`
- `text7-condition-int-invalid.txt`
- `text8-array-index-boolean-invalid.txt`
- `text9-assignment-bool-to-int-invalid.txt`
- `text10-logical-int-invalid.txt`
- `text11-arithmetic-boolean-invalid.txt`
- `text12-int32-to-float64-implicit-cast.txt`
- `text13-float64-to-int32-invalid.txt`
- `text14-mixed-relational-int-float.txt`
- `text15-continue-in-while.txt`
- `text16-continue-in-do-while.txt`
- `text17-continue-outside-loop-invalid.txt`
- `text18-break-outside-loop-invalid.txt`
- `text19-nested-break-continue-binding.txt`
- `text20-double-break-binding.txt`

当前表文件：

- `src/main/resources/serial/lex_table.data`
- `src/main/resources/serial/syntax_table.data`
